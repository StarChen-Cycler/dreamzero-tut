# Testing Rules

- Run `mkdocs build` after structural changes.
- Fix broken links before adding new chapters.
- Confirm that all formulas render in the browser.
- When quoting or paraphrasing the paper, spot-check against `DreamZero-Nvidia.pdf`.
- Treat OCR uncertainty as a test failure until resolved or explicitly marked.
